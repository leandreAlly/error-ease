const addition = (a: number, b: number): number => {
  return a + b;
};
const hello = 10;
const number1: number = 5;
const number2: number = 10;
const newNumber = 8;
const result: number = addition(number1, number2);
// console.log(result);

console.error('result', result, hello, newNumber);
